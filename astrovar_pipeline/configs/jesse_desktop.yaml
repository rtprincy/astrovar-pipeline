
# Gaia and I/O
gaia:
  username: "${GAIA_USER}"      # or set explicitly here
  password: "${GAIA_PASS}"      # or set explicitly here
  tap_url: "https://gea.esac.esa.int/tap-server/tap"
data:
  source_id_list: "../data/id_lists/top1000_stddev.csv"   # CSV with a 'source_id' column
  outdir: "../data"
  cache_dir: "outputs/cache"
  overwrite: false

# Extraction
extraction:
  min_points: 25

# Frequency search
frequency_search:
  min_freq: 0
  max_freq: 60.0
  oversample: 10

# Feature engineering
features:
  augment_with_gaia_summary: true

# Dimensionality reduction & clustering
ml:
  correlation_threshold: 0.95
  tsne:
    perplexity_grid: [30,50]
    learning_rate_grid: [200,300]
    n_iter: 1000
    metric: "euclidean"
    random_state: 42
  gmm:
    n_components_grid: [3,4]
    covariance_type: 'spherical'
    n_init: 10
    random_state: 42
  rf_prune:
    enabled: true
    n_estimators: 100
    top_k_features: 60
    random_state: 42

# Visualization
viz:
  tsne_scatter: true
  feature_importances: true
